# Vite only exposes variables prefixed VITE_ to the browser bundle.
# Set this in Render's static-site environment to the deployed API origin.
# Leave it empty locally so the dev-server proxy in vite.config.js handles /api.
VITE_API_BASE=
